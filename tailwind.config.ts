/**
 * Programa CASS — Tailwind CSS v4
 *
 * Este proyecto usa el pipeline `@tailwindcss/postcss` con tema en CSS
 * (`src/app/globals.css`, bloque `@theme`), incluyendo colores, fuentes y
 * animaciones `animate-fade-up` / `animate-fade-up-slow` (keyframes `fadeUp`).
 *
 * No es necesario importar este archivo en `globals.css` salvo que migres
 * a la directiva opcional `@config` de Tailwind v4.
 */
export {};
